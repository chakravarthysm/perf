use std::sync::{Arc, Mutex};
use std::thread;
use std::sync::mpsc::channel;
const  num: i32 = 10;
struct Resource {
    url: String,
    errCount: i8,
}
fn main() {
    let (pending, complete) = channel::<Resource>();
}
